# 设计指南 - 车险签单数据分析平台 v2.0

> 完整的设计系统文档，包含设计原则、组件规范、配色系统、护眼设计

**目标读者**: UI设计师、前端开发、产品经理
**最后更新**: 2025-11-08

---

## 🎯 设计哲学

### 核心理念

我们的设计遵循**四大原则**：

#### 1. 🛡️ 护眼优先 (Eye Comfort First)
基于眼科医学研究，长时间使用不疲劳

**实践**:
- ✅ 低饱和度配色（饱和度 ≤ 70%）
- ✅ 中等明度（明度 40%-80%）
- ✅ 避免高饱和度纯色（如 `#FF0000`）
- ✅ 充足的留白和间距

**科学依据**:
> 研究表明，长时间盯着高饱和度屏幕会导致视觉疲劳。采用柔和的蓝灰色系可降低眼睛疲劳度 35%。

#### 2. 📊 信息优先 (Information First)
颜色不仅是装饰，更是信息的载体

**颜色编码**:
- 🟢 **绿色** → 上升、积极、成功
- 🔴 **红色** → 下降、警示、需关注
- 🔵 **蓝色** → 主要数据、专业、信任
- ⚪ **灰色** → 辅助数据、中性、背景

**设计逻辑**:
```
用户无需阅读数字，仅凭颜色即可快速感知：
  绿色 ↑ → "数据在上升，做得好！"
  红色 ↓ → "数据在下降，需要关注"
  蓝色 — → "这是主要关注的数据"
  灰色   → "这是参考对比的数据"
```

#### 3. 📐 层次清晰 (Clear Hierarchy)
通过视觉权重引导用户关注重点

**实践**:
- **主要数据**（D 最新周期）: 饱和度高 `#5B8DEF`
- **次要数据**（D-7 上周）: 饱和度中 `#8B95A5`
- **背景数据**（D-14 前周）: 饱和度低 `#C5CAD3`

**视觉心理学**:
> "近重远轻"原则：距离当前时间越近的数据，视觉权重越高，符合人类对时间的认知习惯。

#### 4. 🎯 专业克制 (Professional Restraint)
B端产品追求专业感，避免过度设计

**实践**:
- 参考 Ant Design、Material Design、IBM Carbon
- 使用蓝灰色系而非鲜艳的彩虹色
- 动效克制：仅在必要时使用，持续时间 200-300ms
- 图标简洁：使用线性图标而非填充图标

---

## 🎨 配色系统

### 护眼配色方案

| 用途 | 名称 | 色值 | 使用场景 |
|------|------|------|---------|
| **D (最新周)** | 主蓝色 | `#5B8DEF` | 周对比图D线、主要数据 |
| **D-7 (上周)** | 次灰色 | `#8B95A5` | 周对比图D-7线、次要数据 |
| **D-14 (前周)** | 浅灰色 | `#C5CAD3` | 周对比图D-14线、背景数据 |
| **上升趋势** | 成功绿 | `#52C41A` | 趋势箭头 ↑、增长率文字 |
| **下降趋势** | 警示红 | `#F5222D` | 趋势箭头 ↓、下降率文字 |
| **持平状态** | 中性灰 | `#8B95A5` | 趋势符号 —、持平文字 |

### CSS变量定义

```css
:root {
  /* 图表主色 - 护眼蓝灰系 */
  --chart-primary-blue: #5B8DEF;
  --chart-secondary-gray: #8B95A5;
  --chart-light-gray: #C5CAD3;

  /* 状态色 */
  --status-success: #52C41A;
  --status-warning: #F5222D;
  --status-neutral: #8B95A5;

  /* 中性色 */
  --gray-50: #F9FAFB;
  --gray-900: #111827;
  --text-primary: #1F2937;
  --text-secondary: #6B7280;

  /* 背景色 */
  --bg-primary: #FFFFFF;
  --bg-secondary: #F9FAFB;
}
```

### 应用场景

#### 周对比图表配色
```javascript
// ECharts配色方案
series: [
  {
    name: 'D (11-05): 791万 ↑ 20%',
    color: '#5B8DEF',        // 主蓝色
    lineStyle: { width: 3 }
  },
  {
    name: 'D-7 (10-29): 657万 ↓ 16%',
    color: '#8B95A5',        // 次灰色
    lineStyle: { width: 2 }
  },
  {
    name: 'D-14 (10-22): 781万 — 0%',
    color: '#C5CAD3',        // 浅灰色
    lineStyle: { width: 2, type: 'dashed' }
  }
]
```

#### 动态标签状态
```javascript
// 根据增长率应用颜色
const getTrendColor = (growthRate) => {
  if (growthRate > 5) {
    return { color: '#52C41A', icon: '↑' }  // 成功绿
  } else if (growthRate < -5) {
    return { color: '#F5222D', icon: '↓' }  // 警示红
  } else {
    return { color: '#8B95A5', icon: '—' }  // 中性灰
  }
}
```

---

## 📐 字体系统

### 字体家族

```css
:root {
  --font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
                 "Microsoft YaHei", sans-serif;
}
```

### 字号体系

采用 **1.25倍** 增长比例（Major Third Scale）

```css
:root {
  --text-xs:   12px;   /* 辅助说明、时间戳 */
  --text-sm:   14px;   /* 次要信息、表单标签 */
  --text-base: 16px;   /* 正文、按钮 */
  --text-lg:   20px;   /* 小标题、强调文本 */
  --text-xl:   24px;   /* 二级标题 */
  --text-2xl:  30px;   /* 一级标题 */
  --text-3xl:  36px;   /* KPI数值 */
  --text-4xl:  48px;   /* 首屏大标题 */
}
```

### 字重体系

```css
:root {
  --font-normal:   400;  /* 正文 */
  --font-medium:   500;  /* 次要标题 */
  --font-semibold: 600;  /* 主要标题 */
  --font-bold:     700;  /* 数据强调 */
}
```

---

## 📏 间距系统

采用 **4px基准** 的8点网格系统

```css
:root {
  --space-1:  4px;
  --space-2:  8px;
  --space-3:  12px;
  --space-4:  16px;   /* 基准 */
  --space-5:  20px;
  --space-6:  24px;
  --space-8:  32px;
  --space-10: 40px;
  --space-12: 48px;
}
```

**应用规则**:
- 组件内元素间距：`--space-2` ~ `--space-3`
- 组件间间距：`--space-4` ~ `--space-6`
- 区块间间距：`--space-8` ~ `--space-12`

---

## 🧩 组件规范

### KPI卡片

```vue
<template>
  <div class="kpi-card">
    <div class="kpi-card__icon">📊</div>
    <div class="kpi-card__content">
      <div class="kpi-card__label">签单总保费</div>
      <div class="kpi-card__value">20.5万</div>
      <div :class="['kpi-card__trend', trendClass]">
        {{ trendIcon }} {{ trendText }}
      </div>
    </div>
  </div>
</template>

<style scoped>
.kpi-card {
  background: white;
  border-radius: 12px;
  padding: 24px;
  display: flex;
  align-items: center;
  gap: 20px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
  border-left: 4px solid var(--chart-primary-blue);
}

.kpi-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
}

.kpi-card__value {
  font-size: 36px;
  font-weight: 700;
  color: var(--text-primary);
  font-family: var(--font-mono);
}

.kpi-card__trend--up {
  color: var(--status-success);
}

.kpi-card__trend--down {
  color: var(--status-warning);
}
</style>
```

### Toast通知

```vue
<template>
  <Transition name="toast">
    <div v-if="visible" :class="['toast', `toast--${type}`]">
      <div class="toast-icon">{{ icons[type] }}</div>
      <div class="toast-content">
        <div class="toast-title">{{ title }}</div>
      </div>
      <button class="toast-close" @click="close">×</button>
    </div>
  </Transition>
</template>

<style scoped>
.toast {
  position: fixed;
  top: 20px;
  right: 20px;
  min-width: 320px;
  background: white;
  border-radius: 12px;
  padding: 16px 20px;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
  z-index: 9999;
}

.toast--success {
  border-left: 4px solid var(--status-success);
}

.toast--error {
  border-left: 4px solid var(--status-warning);
}

/* 动画 */
.toast-enter-active,
.toast-leave-active {
  transition: all 0.3s ease;
}

.toast-enter-from {
  transform: translateX(100%);
  opacity: 0;
}
</style>
```

---

## 🎭 动画规范

### 过渡时间

```css
:root {
  --duration-fast:   150ms;  /* 快速交互 */
  --duration-base:   200ms;  /* 基础过渡 */
  --duration-slow:   300ms;  /* 复杂动画 */
  --duration-slower: 500ms;  /* 页面切换 */
}
```

### 缓动函数

```css
:root {
  --ease-out:    cubic-bezier(0, 0, 0.2, 1);
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
}
```

### 常用动画

```css
/* Hover效果 */
.btn {
  transition: all var(--duration-base) var(--ease-out);
}

.btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 16px rgba(0, 0, 0, 0.12);
}
```

---

## 📱 响应式设计

### 断点系统

```css
:root {
  --breakpoint-sm: 640px;   /* 手机 */
  --breakpoint-md: 768px;   /* 平板 */
  --breakpoint-lg: 1024px;  /* 笔记本 */
  --breakpoint-xl: 1280px;  /* 桌面 */
}
```

### 移动端优先

```css
/* 基础样式（移动端） */
.container {
  padding: var(--space-4);
}

/* 平板及以上 */
@media (min-width: 768px) {
  .container {
    padding: var(--space-6);
  }
}

/* 桌面及以上 */
@media (min-width: 1024px) {
  .container {
    padding: var(--space-8);
    max-width: 1400px;
    margin: 0 auto;
  }
}
```

---

## ✅ 设计检查清单

在实现新功能或组件时，请检查：

- [ ] **配色符合护眼原则** - 避免高饱和度颜色
- [ ] **颜色有明确含义** - 绿色=好，红色=警示，蓝色=主要
- [ ] **视觉层次清晰** - 主要信息突出，次要信息弱化
- [ ] **使用统一的CSS变量** - 不直接写颜色值
- [ ] **遵循间距规范** - 使用 4px 倍数间距
- [ ] **动效克制** - 仅在必要时使用，200-300ms
- [ ] **无障碍考虑** - 颜色对比度符合 WCAG AA 标准

---

## 📚 参考资料

### 设计系统参考
- [Ant Design](https://ant.design/docs/spec/colors-cn)
- [Material Design](https://material.io/design/color)
- [IBM Carbon](https://carbondesignsystem.com/)

### 色彩心理学
- [Color Psychology in UX Design](https://www.toptal.com/designers/ux/color-in-ux)

---

**设计理念**: 专业克制，信息优先，护眼为本
**目标用户**: 车险业务团队，长时间数据分析工作者
**成功标准**: 用户满意度>95%，视觉疲劳投诉降低90%

---

## 🔎 筛选面板规范（保单号唯一约束）

### 设计原则：一致性优先
- 选择“保单号”即选择唯一数据实体，其关联的“业务员/三级机构/团队”必须与 `业务员机构团队归属.json` 保持一致。
- 为避免用户制造不一致，选择“保单号”后锁定相关联的维度（禁用手动修改）。
- 若后端检测到数据中的机构/团队与映射不一致，前端以透明提示的方式进行数据治理提醒。

### 组件行为
- 数据源：`GET /api/filter-options`（含保单号列表）与 `GET /api/policy-mapping`（保单→业务员→机构/团队映射）。
- 联动规则：
  - 选择“保单号”→ 自动填充“三级机构/团队”，并在应用筛选时补齐“业务员”。
  - 选择“三级机构”→ 团队选项基于机构联动过滤；切换机构时若当前团队不属于新机构则清空团队。
- 禁用规则：当已选择“保单号”时，禁用“三级机构/团队”的手动修改，避免不一致。

### 校验与提示
- 仪表盘顶部显示“数据映射警告”：
  - 未匹配业务员计数 `validation.unmatched_count`
  - 保单一致性计数 `validation.policy_consistency.mismatch_count`
- 提示文案：
  - “另有 X 个保单的团队/机构与映射不一致，已按映射规则自动校正筛选逻辑。”

### 视觉规范（关键要点）
- 表单控件禁用态：降低不透明度（60%）、禁用手势、背景使用次级灰底。
- 联动选择的过渡：200–300ms 过渡时间，避免突兀变化。
- 提示色：一致性告警采用柔和的警示黄（非高饱和红），强调“治理”而非“错误”。

### 无障碍（A11y）
- 禁用控件须提供 `title` 或辅助说明，解释为什么被锁定。
- 告警区域具备可关闭按钮和语义化角色（如 `role="alert"`）。
